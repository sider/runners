linter:
  rubocop:
    dependencies:
      - rubocop
      - rubocop-minitest
      - rubocop-performance
      - rubocop-rake
      - rubocop-rubycw
      - meowcop
  goodcheck:
    dependencies:
      # Test the edge version.
      - name: goodcheck
        git:
          repo: https://github.com/sider/goodcheck.git
          branch: master
  querly:
    dependencies:
      # Test the edge version.
      - name: querly
        git:
          repo: https://github.com/soutaro/querly.git
          branch: master

ignore:
  - test/smokes/**
